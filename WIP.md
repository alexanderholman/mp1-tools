# Current WIP

Canonical status file: `tasks/wip.md`

Quick snapshot:
- `mlp_tools` package is live with `mp1-id`, `mp1-energies`, `mp1-vasp`.
- Tests currently passing.
- Next implementation target is Artemis migration with `--dry-run` and safer path handling.

If resuming a new session:
1. Read `tasks/wip.md`.
2. Read `../mp1-poly-axis-macro-paper/legacy_processing/01-tools/tools_migration_work_order.md`.
3. Continue at the next unchecked migration step.
